// Super Admin System Test Script\n// Run this in the browser console to test the system\n\nconst testSuperAdminSystem = () => {\n  console.log('🏥 Testing Super Admin Hospital Management System');\n  \n  // Test 1: Check if components are properly imported\n  console.log('✅ Testing component imports...');\n  \n  // Test 2: Check if routes are accessible\n  const routes = [\n    '/super-admin/login',\n    '/super-admin/dashboard', \n    '/super-admin/hospitals',\n    '/super-admin/hospital-admins',\n    '/super-admin/activity'\n  ];\n  \n  console.log('✅ Available Super Admin routes:');\n  routes.forEach(route => console.log(`   - ${route}`));\n  \n  // Test 3: Check if API endpoints are configured\n  const apiEndpoints = [\n    'GET /api/v1/super-admin/dashboard/stats',\n    'GET /api/v1/super-admin/dashboard/activity',\n    'POST /api/v1/super-admin/hospitals',\n    'GET /api/v1/super-admin/hospitals',\n    'POST /api/v1/super-admin/hospital-admins',\n    'GET /api/v1/super-admin/hospital-admins'\n  ];\n  \n  console.log('✅ Available API endpoints:');\n  apiEndpoints.forEach(endpoint => console.log(`   - ${endpoint}`));\n  \n  // Test 4: Check localStorage for authentication\n  const token = localStorage.getItem('token');\n  const user = JSON.parse(localStorage.getItem('user') || '{}');\n  \n  console.log('✅ Authentication status:');\n  console.log(`   - Token: ${token ? 'Present' : 'Missing'}`);\n  console.log(`   - User role: ${user.role || 'Not set'}`);\n  \n  if (user.role === 'super_admin') {\n    console.log('🎉 Super Admin authentication verified!');\n  } else {\n    console.log('⚠️  Super Admin role not detected. Please login as super admin.');\n  }\n  \n  console.log('\\n🚀 Super Admin System Test Complete!');\n  console.log('\\nTo access the system:');\n  console.log('1. Navigate to /super-admin/login');\n  console.log('2. Login with super admin credentials');\n  console.log('3. Access dashboard at /super-admin/dashboard');\n};\n\n// Auto-run test if in browser\nif (typeof window !== 'undefined') {\n  testSuperAdminSystem();\n}\n\nexport default testSuperAdminSystem;